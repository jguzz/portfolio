import styled from "styled-components";

const Nav = styled.div`
  display: flex;
  flex: 18%;
  background-color: ${props => props.theme.primary};
  text-align: center;
  height: 100vh;
`;

export default Nav;
